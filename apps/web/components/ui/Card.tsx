import { cn } from '@/lib/utils';
export function Card({ className, children }: { className?: string; children: React.ReactNode }) { return <div className={cn('rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm', className)}>{children}</div>; }
export function SectionTitle({ eyebrow, title, description, light=false }: { eyebrow?: string; title: string; description?: string; light?: boolean }) { return <div className="mx-auto max-w-3xl text-center">{eyebrow && <p className={cn('mb-3 text-sm font-black tracking-wide', light ? 'text-cyan-200' : 'text-raqaba-600')}>{eyebrow}</p>}<h2 className={cn('text-3xl font-black leading-tight md:text-5xl', light ? 'text-white' : 'text-ink')}>{title}</h2>{description && <p className={cn('mt-5 text-lg leading-8', light ? 'text-blue-100' : 'text-slate-600')}>{description}</p>}</div>; }
