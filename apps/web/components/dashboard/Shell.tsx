'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { Activity, Bell, Boxes, ChartSpline, ClipboardList, CreditCard, Home, LayoutDashboard, LogOut, PackageSearch, ReceiptText, Settings, ShoppingCart, Truck, UsersRound, WalletCards, Warehouse } from 'lucide-react';
import { cn } from '@/lib/utils';
import { useAuth } from './AuthProvider';
const nav=[['/dashboard','نظرة عامة',LayoutDashboard],['/dashboard/products','المنتجات',Boxes],['/dashboard/categories','التصنيفات',PackageSearch],['/dashboard/inventory','المخزون',Warehouse],['/dashboard/sales','المبيعات',ReceiptText],['/dashboard/purchases','المشتريات',ShoppingCart],['/dashboard/customers','الزبائن',UsersRound],['/dashboard/suppliers','الموردون',Truck],['/dashboard/expenses','المصاريف',WalletCards],['/dashboard/reports','التقارير',ChartSpline],['/dashboard/staff','الفريق والصلاحيات',ClipboardList],['/dashboard/activity','سجل النشاط',Activity],['/dashboard/subscription','الاشتراك',CreditCard],['/dashboard/settings','الإعدادات',Settings]];
export function DashboardShell({children}:{children:React.ReactNode}){const path=usePathname(); const {user,business,logout}=useAuth(); return <div className="min-h-screen bg-slate-50 text-ink"><aside className="fixed right-0 top-0 z-30 hidden h-screen w-72 border-l border-slate-200 bg-white p-4 lg:block"><Link href="/" className="mb-6 flex items-center gap-3 rounded-3xl bg-raqaba-950 p-4 text-white"><Home/><div><b className="block text-xl">رقابة</b><span className="text-xs text-blue-200">{business?.name}</span></div></Link><nav className="h-[calc(100vh-140px)] space-y-1 overflow-y-auto no-scrollbar">{nav.map(([href,label,Icon]: any)=>{const active=path===href; return <Link key={href} href={href} className={cn('flex items-center gap-3 rounded-2xl px-4 py-3 text-sm font-bold transition',active?'bg-raqaba-600 text-white shadow-lg shadow-raqaba-600/20':'text-slate-600 hover:bg-slate-100')}><Icon size={19}/>{label}</Link>})}</nav></aside><div className="lg:pr-72"><header className="sticky top-0 z-20 border-b border-slate-200 bg-white/85 backdrop-blur-xl"><div className="flex items-center justify-between px-5 py-4"><div><p className="text-sm text-slate-500">مرحباً، {user?.fullName}</p><h1 className="text-xl font-black">{business?.name}</h1></div><div className="flex items-center gap-3"><Link href="/dashboard/notifications" className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-100 text-slate-700"><Bell size={20}/></Link><span className="hidden rounded-2xl bg-raqaba-50 px-3 py-2 text-sm font-bold text-raqaba-800 md:block">{user?.role}</span><button onClick={logout} className="grid h-11 w-11 place-items-center rounded-2xl bg-rose-50 text-rose-600"><LogOut size={20}/></button></div></div><div className="flex gap-2 overflow-x-auto px-4 pb-3 lg:hidden">{nav.slice(0,10).map(([href,label]: any)=><Link key={href} href={href} className={cn('whitespace-nowrap rounded-full px-4 py-2 text-sm font-bold',path===href?'bg-raqaba-600 text-white':'bg-slate-100')}>{label}</Link>)}</div></header><main className="p-5 md:p-8">{children}</main></div></div>}
